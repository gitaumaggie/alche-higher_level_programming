
PYTHON -CLASSES AND OBJECTS:

This project introduces object-oriented programming in python. By doing tasks aligning to the concept, one learns how classes act as blueprints and  by creating objects learns how objects contain attributes and methods. The project also introduces the purpose of self and _init_ as well as abstraction, encapsulation and information hiding.

REQUIREMENTS:

git and github
ubuntu 20.04
python3
