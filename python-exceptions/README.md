# Python - Exceptions

This project introduces me to handling errors and exceptions in Python. I learned the difference between errors and exceptions, when exceptions occur, and how to handle them using `try` and `except`. I also learned how to raise built-in exceptions with `raise` and use `finally` for clean-up actions. As I continue developing my Software Engineering skills, exception handling will help me build programs that respond safely to unexpected situations instead of crashing and provide users with meaningful feedback.

PYTHON EXCEPTIONS:

This project introduces the concept of handling errors and exceptions in python. It involves performing tasks that help one learn the difference between errors and exceptions, when exceptions occur , and how to handle them using  try and except.It finally brings in the concept of how to raise built-in exceptions with raise and use finally for clean -up actions.

REQUIREMENTS:

git and github
ubuntu 20.04
python3
